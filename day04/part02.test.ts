import { describe, expect, it } from "bun:test";

describe("day04/part02", () => {
  it("description", () => {
    const expected = null;

    const actual = null;

    expect(actual).toEqual(expected);
  });
});
